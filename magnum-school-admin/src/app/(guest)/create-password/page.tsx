import React from 'react';
import CreatePasswordForm from '@/views/auth/createPassword/CreatePasswordForm';

const page = () => {
  return (
    <div>
      <CreatePasswordForm />
    </div>
  );
};

export default page;
