import React from 'react';
import CreatePasswordForm from './CreatePasswordForm';

const page = () => {
  return (
    <div>
      <CreatePasswordForm />
    </div>
  );
};

export default page;
